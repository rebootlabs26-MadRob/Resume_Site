
Efficient Terminal Navigation + Daily Ops

🔹 Navigation & Filesystem

• `ls` — list files  
• `cd` — change directory  
• `cd ..` — up one directory  
• `pwd` — show current directory  
• `mkdir` — create folder  
• `rm` — delete file  
• `rm -r` — delete folder recursively  
• `cp` — copy  
• `mv` — move/rename

🔹 System & Process Control

• `Get-Process` — list processes  
• `Stop-Process -Name` — kill process  
• `Get-Service` — list services  
• `Start-Service` — start service  
• `Stop-Service` — stop service

🔹 Networking

• `ipconfig` — show IP info  
• `Test-Connection` — ping  
• `Get-NetAdapter` — list NICs  
• `Get-NetIPAddress` — show IPs  
• `Resolve-DnsName` — DNS lookup

🔹 Package Management

• `winget search`  
• `winget install`  
• `winget upgrade --all`

🔹 PowerShell Essentials

• `Get-Help -Online` — full docs  
• `Get-Command` — find commands  
• `Get-Alias` — list aliases  
• `history` — show command history  
• `clear` — clear terminal

🔹 Useful Shortcuts

• `Ctrl + L` — clear screen  
• `Ctrl + C` — cancel running command  
• `Tab` — autocomplete  
• `Up Arrow` — previous command